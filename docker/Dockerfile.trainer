FROM python:3.10-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY docker/requirements/requirements-train.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy trainer script and data
COPY model_trainer.py ./
COPY data/ ./data/

# Shared volume will be mounted at /shared-volume by Kubernetes
RUN mkdir -p /shared-volume

CMD ["python", "model_trainer.py"]`
